[pytest]
asyncio_mode = auto
testpaths = python_core/tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
markers =
    heavy: tests that load large models (~minutes to run; deselect with '-m "not heavy"')
